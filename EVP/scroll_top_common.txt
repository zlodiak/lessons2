если сколл по  main, то прокрутить его до верхней позиции можно так:

$("#main ").animate({
    scrollTop: 0
}, "slow");



если возврат происходит на главную старничку, то нужен таймаут чтобы успел построиться DOM