клиенты не должны принудительно внедрять интерфейсы, которые они не используют.

в 0:i_wrong.js пример, в результате которого рабочий может увеличивать себе зарплату.
если зарплата будет храниться в БД, то скорее всего система прав не позволит сотруднику увеличивать себе зар.плату, но в любом случае раздутый интерфейс принуждает класс реализовать метод setPay()

в примере 0:i_right.js исправленная версия, в которой интерфейс разделён на 2 части.

также показательным может быть пример из каткалога 1:. видно, что классу circle незачем реализовывать метод отрисовки квадрата.