delay откладывает передачу значения в subscribe на указанное время

    of(3, 6, 9).pipe(
     delay(2000),
    ).subscribe(vl => console.log(vl));
    
    //Результат (через 2 секунды): 3, 6, 9

==========
==========
==========
==========